#include "lists.h"
/**
 * print_listint_safe - a function that print listint_t
 * @head: head
 * Return: 0
 */
size_t print_listint_safe(const listint_t *head)
{
	if (head == NULL)
		exit(98);

	return (0);
}
