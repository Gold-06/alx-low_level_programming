#include "main.h"
/**
 * _abs - a functions that print the absolute value of an integer
 * @ab: integer input
 * Return: Always 0 (Success)
 */
int _abs(int ab)
{
	return (ab * ((ab > 0) -(ab < 0)));
}
